import { CssBaseline } from '@mui/material';
import AppContainer from './main/modules/app/AppContainer.tsx';

const AppRootContainer = () => (
   <>
      <CssBaseline />
      <AppContainer />
   </>
);

export default AppRootContainer;
